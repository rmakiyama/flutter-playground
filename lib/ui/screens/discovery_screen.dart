import 'package:flutter/material.dart';

class DiscoveryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("DiscoveryScreen"),
      ),
      body: Center(
        child: Text("DiscoveryScreen"),
      ),
    );
  }
}
